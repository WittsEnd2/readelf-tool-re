use std::env;
fn main() {
    let args: Vec<String> = env::args().collect();
    if () 
    for arg in args 
    {
        if arg.find("-") != None {
            println!("{:?}", arg);
        }
        println!("{:?}", arg);
    }
}

